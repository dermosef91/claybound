// STUB — replaced wholesale by the dream primitives' real views. Kept to two
// exports so dist/dream.js and world.js can already import them.
export function createDreamView(w,s,g){return null;}
export function animateDreamViews(w,game,dt){}
