# Ember Caverns canonical layout v7

Promoted from the latest `/Users/moritzgrassy/Downloads/claybound-chapter-3.json` export.

The export is preserved as `editor-backup.json`; `editor-backup-v6.json` and
`previous-canonical-v6.json` retain the prior canonical state. `changes-v7.json`
records the field-level platform changes and list counts.

This revision contains 103 platforms, 38 beads, five secret flowers, seven
enemies, and three presses. The canonical editor regression verifies every
editable field against the export and the runtime/editor UI checks pass.

The export's explicit route metadata is preserved and filtered to existing
platforms. The generic lowland route pilot is not a gate for this revision
because the new wall climbs require editor-authored vertical timing.
