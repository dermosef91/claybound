node_modules/
.pnpm-store/
.DS_Store
*.log
.env
.env.*
!.env.example

# Optional production bundle (npm run build); dist/ remains the source.
build/

# Firebase CLI upload cache, rewritten on every deploy. firebase.json and
# .firebaserc are the configuration and are tracked.
.firebase/

# Test output: rewritten by tests/playthroughs.mjs on every successful run.
tests/playthrough-results.json
tests/flower-playthrough-results.json

# Blender auto-save backups left beside .blend files by asset preparation.
*.blend1
