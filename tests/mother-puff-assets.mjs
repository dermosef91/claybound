import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';
import {createHash} from 'node:crypto';
import {parseHTML} from 'linkedom';
import * as THREE from '../dist/lib/three.module.js';
import {World} from '../dist/world.js';
import {Game} from '../dist/simulation.js';
import {prepareMotherPuff,createMotherPuff,animateMotherPuff,motherCamera,motherViewHeight} from '../dist/mother-puff.js';
import {updateMotherAtmosphere} from '../dist/mother-puff-hud.js';
import {disposeBranch} from '../dist/streaming.js';
import {readGLB} from './load-player.mjs';
import {attachClay} from './load-clay.mjs';

const w=Object.assign(Object.create(World.prototype),{scene:new THREE.Scene(),levelRoot:new THREE.Group(),mat:{},reducedMotion:false});
for(const name of ['cream','bark','top'])w.mat[name]=new THREE.MeshStandardMaterial();
await attachClay(w);
for(const [pose,triangles]of [['idle',10448],['cast',10428]]){
 const url=new URL(`../dist/assets/mother-puff-${pose}.glb`,import.meta.url),gltf=await readGLB(url),bytes=await readFile(url),manifest=JSON.parse(await readFile(new URL(`../dist/assets/mother-puff-${pose}.json`,import.meta.url)));
 assert.equal(createHash('sha256').update(bytes).digest('hex'),manifest.shippedSha256);assert.equal(bytes.length,manifest.shippedBytes);assert(bytes.length<1100000);assert.equal(manifest.triangles,triangles);assert(manifest.geometryUnchanged);
 prepareMotherPuff(w,pose,gltf);const asset=w.motherAssets[pose];assert(asset.scale>4);assert.equal(gltf.animations.length,0);
 let count=0;asset.scene.traverse(o=>{if(o.isMesh){count+=o.geometry.index.count/3;assert(o.material.map&&o.material.normalMap&&o.material.roughnessMap);assert(w.assetGeometry.has(o.geometry));assert(w.assetMaterials.has(o.material));}});assert.equal(count,triangles);
}
const game=new Game();game.start(1);const b=game.level.boss;Object.assign(game.player,{x:281.5,y:b.y,groundId:'mother-arena'});
const v=w.motherView=createMotherPuff(w,b);animateMotherPuff(w,game);assert(v.models.idle.visible&&!v.models.cast.visible);
v.root.updateMatrixWorld(true);
for(const m of Object.values(v.models)){const box=new THREE.Box3().setFromObject(m,true);assert(Math.abs(box.min.y-b.y)<.001);assert(Math.abs(box.max.y-b.y-6.4)<.001);}
let disposed=0;for(const r of [...w.assetGeometry,...w.assetMaterials])r.addEventListener('dispose',()=>disposed++);
b.state='release';b.stateTime=.25;game.time=2.5;animateMotherPuff(w,game);assert(!v.models.idle.visible&&v.models.cast.visible);assert(v.gates.every(g=>g.visible));
game.pause();const pose=()=>JSON.stringify([v.pose.scale.toArray(),v.pose.rotation.toArray()]);const frozen=pose();game.tick(3);animateMotherPuff(w,game);assert.equal(pose(),frozen);
w.reducedMotion=true;b.state='sleeping';animateMotherPuff(w,game);const still=pose();game.time=20;animateMotherPuff(w,game);assert.equal(pose(),still);w.reducedMotion=false;
b.state='recover';b.spores=[{id:12,color:'orange',x:287,y:39,targetX:285.1,targetY:b.y,age:.4,duration:1.85}];animateMotherPuff(w,game);assert.equal(v.effects.size,1);
const air=v.effects.get('air:12');let rings=0;air.marker.traverse(o=>o.geometry?.addEventListener('dispose',()=>rings++));b.spores=[];b.patches=[{id:12,color:'orange',x:285.1,y:b.y,age:1,life:12,bounceAge:10}];animateMotherPuff(w,game);assert.equal(v.effects.size,1);assert(!air.marker.parent&&!air.root.parent);assert.equal(rings,1);
const cap=v.effects.get('ground:12');b.patches=[];animateMotherPuff(w,game);assert.equal(v.effects.size,0);assert(!cap.root.parent);
b.state='defeated';b.stateTime=2;animateMotherPuff(w,game);assert(!v.gates.some(g=>g.visible));assert(!v.pose.visible);
disposeBranch(w,v.root);assert.equal(disposed,0,'unloading the clearing retains the supplied models and shared materials');
w.motherView=createMotherPuff(w,b);animateMotherPuff(w,game);assert(w.motherView.models.idle.visible);assert.equal(disposed,0);
const {document}=parseHTML(await readFile(new URL('../dist/index.html',import.meta.url),'utf8')),mist=document.getElementById('mother-mist');
assert.equal(document.getElementById('mother-hud'),null,'no boss name, description or health bar');
b.state='recover';b.hits=1;game.player.sporeSlow=1;game.status='playing';updateMotherAtmosphere(game,mist,true);assert(!mist.classList.contains('hidden'));
updateMotherAtmosphere(game,mist,false);assert(mist.classList.contains('hidden'));game.player.sporeSlow=0;updateMotherAtmosphere(game,mist,true);assert(mist.classList.contains('hidden'));
const view=w.motherView;
b.hits=0;b.state='recover';b.healing=0;animateMotherPuff(w,game);assert.equal(view.growth.bands.filter(x=>x.root.visible).length,3);
view.root.updateMatrixWorld(true);const swollen=new THREE.Box3().setFromObject(view.growth.bands[0].root,true);assert(swollen.max.y>b.y+8,'the swollen silhouette towers above the supplied normal body');
const baseColor=w.motherAssets.idle.scene.children[0]?.material?.color?.getHex();
b.hits=1;animateMotherPuff(w,game);assert.equal(view.growth.bands.filter(x=>x.root.visible).length,2);assert(view.bodyGrey.value<.27);
b.hits=2;animateMotherPuff(w,game);assert.equal(view.growth.bands.filter(x=>x.root.visible).length,1);
b.hits=3;b.state='quiet';animateMotherPuff(w,game);assert.equal(view.growth.bands.filter(x=>x.root.visible).length,0);assert.equal(view.pose.scale.y,.15);assert(!view.friendly.root.visible);
b.state='revive';b.stateTime=2.8;animateMotherPuff(w,game);assert.equal(view.pose.scale.y,.36);assert(view.friendly.root.visible);
b.state='bloom';b.stateTime=2;b.healing=.6;animateMotherPuff(w,game);assert(!view.pose.visible);assert(view.clearing.flowers.some(f=>f.petals[0].pivot.rotation.x>1));assert(view.clearing.flowers.some(f=>f.petals[0].pivot.rotation.x===0),'flowers open in an outward wave');assert(view.sceneryGrey.value<1);
b.state='defeated';b.healing=1;animateMotherPuff(w,game);assert(!view.friendly.root.visible);assert(view.clearing.flowers.every(f=>f.petals[0].pivot.rotation.x>1));
assert.equal(w.motherAssets.idle.scene.children[0]?.material?.color?.getHex(),baseColor,'original asset material stays unchanged');
let afflicted;view.models.idle.traverse(o=>{if(o.isMesh)afflicted=o.material;});
const shader={uniforms:{},vertexShader:THREE.ShaderLib.standard.vertexShader,fragmentShader:THREE.ShaderLib.standard.fragmentShader};afflicted.onBeforeCompile(shader);
assert(shader.fragmentShader.includes('motherGrey'));assert(shader.fragmentShader.includes('claySurface'));assert(shader.uniforms.motherCorruption===view.bodyGrey,'corruption preserves the clay shading and uses view-owned state');
const portrait=motherCamera(b,{...game.player,x:285.1},18.2,39.4,false);assert(Math.abs(portrait.x-b.x)<3);assert.equal(motherCamera(b,{x:1},30,18,true),null);
b.state='reveal';b.hits=0;b.stateTime=3.4;
const introHeight=motherViewHeight(b,390,844,false),introWidth=introHeight*390/844,intro=motherCamera(b,{x:b.triggerX,y:b.y},introWidth,introHeight,false);
assert(intro.x-introWidth/2<b.triggerX-.32&&intro.x+introWidth/2>b.x+3.5,'portrait reveal contains the player and the entire swollen cap');
b.state='farewell';b.hits=3;b.stateTime=.8;animateMotherPuff(w,game);
assert(view.friendly.parts.every(m=>m.scale.x<.35),'friendly spores retain their small sculpted scale');
console.log('PASS Mother Puff assets: both supplied GLBs/maps/fingerprints, grounding, pose switch, pause/reduced motion, bounded effects, shared-resource cleanup, HUD removal, phase silhouettes, small recovery, outward flower opening, isolated material restoration and portrait framing');
